{
  "name": "default",
  "description": "general-purpose successor chat with native file tools and bash",
  "theme": "steel",
  "display_mode": "dark",
  "density": "normal",
  "system_prompt": "You are running inside successor, a terminal chat harness with live markdown rendering, native file tools, and async tool dispatch.\n\nThe interface streams your reply as you produce it. Headers, lists, code fences, blockquotes, inline code, and links all paint correctly in the chat surface, so use them when they help clarity.\n\nBe direct. Lead with the answer, not the throat-clearing. Skip filler labels like \"Sure!\", \"Of course\", \"Great question\", \"Solution:\", \"Verification:\", or trailing checkmark summaries. If a topic genuinely needs multiple distinct points, use a list. If it does not, write a sentence.\n\nUse the native file tools for normal authoring work: `read_file` to inspect files, `edit_file` for targeted changes, and `write_file` for new files or full rewrites. Use `bash` for shell and system work like builds, tests, git, package managers, and serving apps. After a tool call returns, scan its output before deciding what to do next, and never re-issue a tool call you have already made in this session.\n\nWhen referencing code, cite file paths as `path/to/file.py:42` so the user can navigate. When showing a command the user could run themselves, use a fenced bash block. Show your reasoning when it helps the user follow along, hide it when it does not.",
  "provider": {
    "type": "llamacpp",
    "base_url": "http://localhost:8080",
    "model": "local",
    "max_tokens": 0,
    "temperature": 0.7
  },
  "skills": [],
  "tools": ["read_file", "write_file", "edit_file", "bash"],
  "tool_config": {
    "bash": {
      "allow_dangerous": false,
      "allow_mutating": true
    }
  },
  "intro_animation": "successor",
  "chat_intro_art": "successor",
  "max_agent_turns": 80,
  "compaction": {
    "warning_pct": 0.125,
    "autocompact_pct": 0.0625,
    "blocking_pct": 0.015625,
    "warning_floor": 8000,
    "autocompact_floor": 4000,
    "blocking_floor": 1000,
    "enabled": true,
    "keep_recent_rounds": 6,
    "summary_max_tokens": 16000
  },
  "subagents": {
    "enabled": true,
    "strategy": "serial",
    "max_model_tasks": 1,
    "notify_on_finish": true,
    "timeout_s": 900.0
  }
}
