"""Successor — terminal chat harness for local LLMs and OpenAI-compatible
endpoints, with a custom cell-based renderer, async bash tool dispatch,
and live compaction.

The package version is the single source of truth for both the
`successor --version` CLI output and the pyproject.toml build metadata.
Keep them in sync.
"""
__version__ = "0.1.8"
